spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
supabase:
  url: https://pcwbndmvzvzbthdxqmhf.supabase.co
  key: ${DB_KEY}
  bucket: images

jwt:
  # 환경변수 JWT_SECRET_KEY가 있으면 그거 쓰고, 없으면 뒤에 있는 기본값(로컬용)
  secret: ${JWT_SECRET_KEY:v3rY_s3cr3t_k3y_f0r_l0c4l_d3v_0nly_d0_n0t_us3_1n_pr0d}